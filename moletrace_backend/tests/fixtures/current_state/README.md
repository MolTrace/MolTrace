# Current-State Guard Fixtures

These small fixtures define the stable baseline used by
`tests/test_current_state_guard.py`.

The guard suite is deliberately independent of the 2D NMR feature. New modules
must not change existing endpoint schemas or stable scientific defaults unless
that change is explicit and these snapshots are intentionally updated.
