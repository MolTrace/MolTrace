[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=src uv run uvicorn nmrcheck.web:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
